//! string
//? A string is a sequence of characters wrapped within double quotes, single quotes or backticks.
const str1 = "Hey I am a string inside single quotes";
const str2 = "Hey I am a string inside single quotes";
const str3 = `Hey I am a string inside single quotes`;
let myName = "Shanu";
let strWithTemplateLiterals = `My Name is ${myName}`;
console.log(strWithTemplateLiterals);

//! number
//? Number datatype represents both integer and floating value in TypeScript.

//! Whenever this will be saved in memory it will be saved like 24.0. In memory it will be saved like a floating point value.
let num = 24;
console.log(num);
let pi = 3.14;
console.log(pi);

//! Boolean
//? Boolean datatype is used to store either a value true or false. It does not have any other value.
let isEligible = true;
let isEqual = false;
